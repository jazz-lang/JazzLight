var thread = $thread_spawn(function() -> {
    var fac = function(x) -> {
        if x == 0 {
            return 1 
        } else {
            return fac(x - 1) * x
        }
    }

    return fac(5)
    
})



$print($thread_join(thread))